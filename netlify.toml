[build]
  command = "corepack enable && pnpm install && pnpm build && pnpm --filter docs build"
  publish = "docs/.next"

[build.environment]
  NODE_VERSION = "22"

[[plugins]]
  package = "@netlify/plugin-nextjs"
